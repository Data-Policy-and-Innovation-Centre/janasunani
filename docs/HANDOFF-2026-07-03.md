# Session handoff — 2026-07-03

For the next agent/session picking this up cold. `docs/ROADMAP.md` is the source of
truth for the plan; this file is the *session state* that isn't in the roadmap yet.
Maintainer: single person (yashaswi@uchicagotrust.org), demo deadline end of July 2026.

## Where things stand right now

**Branch / PR state (chain, oldest first):**
- `main` — has Weeks 1–2: Terraform CPU box, verified cloud migration, compose `oltp`,
  CI with Postgres service, DVC-tracked dump/mappings. (PRs #2, #3 merged.)
- **PR #4** (`feat/phase5-pipeline` → main) — OPEN, CI green, **awaiting maintainer
  merge**. Full Phase 5: pipeline refold, OLTP exporter, model mirrors, loguru
  conversion, two rounds of Codex review fixes (10 fixed, 5 deferred with rationale
  in ROADMAP "Backfill hardening" open item).
- **`feat/presidio-pii`** (stacked on #4, pushed, no PR yet) — the current tip.
  PII stage rebuilt on Presidio; open its PR **after** #4 merges.

**Work queue (in order):**
1. Merge #4 (maintainer), then open PR for `feat/presidio-pii`.
2. **GPU box** — new branch off main: Terraform g6.xlarge from a Deep Learning AMI
   (count-toggled or separate apply in `deploy/terraform/`), nvidia-container-toolkit,
   build the `ocr-deepseek` env (`uv sync --extra ocr-deepseek` — it is DECLARED
   CONFLICTING with pipeline-core/categorizer via `[tool.uv].conflicts`; separate env/
   image per extra). Smoke DeepSeek on `data/raw/documents-sample` (dvc pull). Watch:
   `trust_remote_code` may import flash_attn; transformers pinned 4.46.3 there.
   Adopt the report's repetition-collapse check (top-trigram share > 0.5 ⇒ bad page).
3. **~200-doc sample backfill** on the GPU box → `janasunani-export-pipeline` → OLTP
   → `janasunani-materialize`. Pick STANDARD-storage-class objects only — parts of
   s3://janasunani-documents-main are GLACIER-archived. Preserve directory structure
   (ticket numbers are path-derived; see documentation in the DSI repo and
   `janasunani/pipeline/ticket.py`).
4. **PII eval (pre-backfill TODO in ROADMAP)** — label ~100–200 real pages, measure
   Presidio coverage vs the legacy 80.56% overlap baseline; upgrade NER if PERSON lags.
5. MLflow slim (Phase 6 folded): local backend on CPU box, artifacts to S3.
6. Then Week 3: API-contract-first (Phase 10 skeleton w/ mocked processor → frontend
   scaffold → Phases 8/9 fill in). See ROADMAP.

**Blocked on the maintainer:** Janasunani API credentials (ingestion smoke — API
pulls currently unavailable anyway).

## Live infrastructure (real, costs money)

- **CPU box**: `ssh ubuntu@52.66.116.80` (Elastic IP; t3.large; instance
  `i-0ef24e15a80ba7128`, ap-south-1, account 966452703664). Managed by
  `deploy/terraform/` (state + tfvars are LOCAL on the maintainer's machine,
  gitignored). Working copy at `~/janasunani` (currently on `feat/phase5-pipeline`).
- **SSH is IP-locked** to `admin_cidr` in `deploy/terraform/terraform.tfvars`. The
  maintainer's IPv4 ROTATES (VPN on/off) — if SSH times out, check
  `curl -4 -s ifconfig.me` vs tfvars, update, `terraform apply` (~5 s). Use
  `curl -4`; plain curl returns IPv6 here.
- **GitHub auth on the box = SSH agent forwarding** (`ssh -A`; run
  `ssh-add --apple-use-keychain ~/.ssh/id_ed25519` locally first — agent often
  starts empty). Repo AND the `dpic` pyproject dep are private; no credential
  lives on the box.
- **Postgres** (`janasunani-oltp` container, compose-managed via
  `deploy/docker-compose.yml`, localhost-only): full migrated history —
  **1,371,288 complaints / 6,556,171 action_history** (verified exact). Password
  only in the box's `.env` + `deploy/.env`. NEVER `docker compose down -v`.
  Nightly pg_dump → s3://grievance-database-backups-main/janasunani/ (cron 21:30 UTC).
- **NEVER run pytest on the box against the prod DB** — the Postgres test fixtures
  DROP TABLES. Tests default to a throwaway postgres:16 on 127.0.0.1:5433.

## Conventions (the maintainer holds these)

- **Testing policy**: every feature ships with real-code-path pytest, run green
  before "done". Gate = `uv run pytest && uv run ruff check .` (49 passed currently).
  Local env: `uv sync --extra pipeline-core`. CI runs base env only (heavy-extra
  tests must skip gracefully — keep test-imported helpers in light modules;
  see tests/conftest.py for the macOS OMP/xgboost-vs-spacy segfault pin).
- **Commits**: meaningful clusters, conventional-commit style, explain the WHY;
  trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` (adapt author).
  Commit/push only when asked or clearly part of the established flow.
- **Codex bot reviews every PR** — its findings have been consistently good. Loop:
  triage each comment (valid-fix / valid-defer-with-rationale / decision-stands),
  fix in clusters, record deferrals in ROADMAP open items.
- **DVC boundary**: file artifacts + reproducible file-in/file-out transforms only
  (`pipeline-sample`, `materialize` stages); operational jobs (migration, backfill,
  ingestion) are CLIs/cron, never `dvc repro`. Remote = s3://dpic-dvc-cache/janasunani
  (IAM-scoped to this prefix). `dvc repro` only works with local SQLite OLTP.
- **Model provenance rule**: stages load from our DVC mirrors under `models/`
  (categorizer, page-type ViT, format pickle) or big public repos (bart-large-cnn,
  DeepSeek-OCR). Never from DSI accounts (team disbanded; their Box is gone —
  PII CRF weights unrecoverable, which is why Presidio; training loop survives at
  DSI-repo commit `db4885f`; measured baselines live in ROADMAP + the DSI report PDF).
- **Postgres portability lessons** (all bitten already): no NUL bytes in text
  (schema layer strips), no unbounded text in btree keys (md5 it — `dedup_remark`
  precedent), authenticated MySQL readiness probe, asyncpg strictness.
- ROADMAP.md is kept in sync with reality — update it when state changes.

## Today's session in one paragraph

Merged week-1/2 PRs; shipped Phase 5 (refold + exporter + mirrors + loguru) through
two Codex review rounds; discovered the DSI team disbanded → mirrored their public
HF models into our DVC remote before they vanish, rebuilt the PII stage on Presidio
(typed tokens, no truncation window, mixed-language coverage, 4 tests + live smoke
on real pages), recorded the DSI report's measured baselines (legacy PII coverage
80.56% overlap / 50% exact = the number to beat; MuRIL trained on only 6.5K ⇒
retrain-on-our-1.37M is the post-demo upgrade), and queued the GPU box as the next
build item.

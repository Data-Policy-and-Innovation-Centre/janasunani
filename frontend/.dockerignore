node_modules
.next
tsconfig.tsbuildinfo
Dockerfile
.dockerignore
# Never let a local dev .env.local (or any local env override) leak into the
# build context; NEXT_PUBLIC_API_URL is supplied via the Dockerfile ARG.
.env*
